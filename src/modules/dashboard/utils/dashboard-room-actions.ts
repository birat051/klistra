/** Client-side document row mutations; replace with server actions when wired. */
export function renameDashboardRoom(roomId: string, newName: string): void {
  void roomId
  void newName
  /* TODO: server action / mutation */
}

export function deleteDashboardRoom(roomId: string): void {
  void roomId
  /* TODO: server action / mutation */
}
