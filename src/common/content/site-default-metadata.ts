/** Default English SEO strings shared by `app/layout.tsx` and `LANDING_PAGE_SEO.en`. */
export const SITE_DEFAULT_TITLE = 'Klistra — think together, online or off' as const

export const SITE_DEFAULT_DESCRIPTION =
  "Klistra keeps everyone's notes in one room — rich text, peer cursors, and rewindable history. Edits keep working when the wifi doesn't." as const
